window.addEventListener('load', function() {
    socketIOLoaded = true;    
    console.log('window loaded');
    gameInstance.SendMessage('Obvyazka3', 'SocketIOStarted' );
});

function findGetParameter(parameterName) {

    return result;
}    